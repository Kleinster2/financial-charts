name: Refiners
primary: VLO
prefix: refiners-cluster
threshold: 0.5
window_end: 2026-06-18
history_start: 2020-01-01

groups:
  cluster:                      # candidate: US refiners (independent + integrated-downstream)
    color: "#2962FF"
    tickers: [VLO, MPC, PSX, PBF, DK, DINO]
  majors:                       # crude-beta pole (validated oil-majors cohort)
    color: "#7E57C2"
    tickers: [XOM, CVX, COP, EOG]
  services:                     # the other value-chain leg (validated OFS cohort)
    color: "#26A69A"
    tickers: [SLB, HAL, BKR]
  etf:                          # refiners ETF (replicability) + energy + crude + market
    color: "#9E9E9E"
    tickers: [CRAK, XLE, USO, SPY]
