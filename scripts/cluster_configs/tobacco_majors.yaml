# Tobacco majors cluster — the four big Western multinational tobacco/nicotine brand-owners.
# The campaign's first defensive / low-vol cohort. Core question is the VOL-MATCHED null:
# does a high-dividend sin-stock basket cohere as a real tobacco-specific factor, or is it a
# bond-proxy duration basket that co-moves only because all four are low-vol yield names (the
# same failure shape as China special valuation)? Controls test the two defensive alternatives:
# XLP = is it just staples, XLU = is it just generic bond-proxy defensive / duration.
# IMBBY is a US OTC ADR tracking the London close — watch the weekly-vs-daily gap for async.
name: Tobacco majors
primary: MO
prefix: tobacco-majors-cluster
threshold: 0.5
window_end: 2026-06-12
definition_date: 2026-06-14
groups:
  cluster:
    color: "#2962FF"
    tickers: [MO, PM, BTI, IMBBY]
  staples_etf:
    color: "#26A69A"
    tickers: [XLP]
  defensive_duration:
    color: "#7E57C2"
    tickers: [XLU]
  etf:
    color: "#9E9E9E"
    tickers: [SPY]
