{
  "name": "financial-charts",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  },
  "scripts": {
    "test:consistency": "python scripts/check_skill_parity.py --all-scopes --strict --optional-openclaw && python tests/note_compliance_regression.py && python tests/kalshi_watchlist_tests.py && python tests/cluster_statistics_tests.py && python scripts/check_note_compliance.py --market-reaction-peer-sweep && python scripts/audit_note_taxonomy.py",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:smoke": "playwright test --project=chromium",
    "test:smoke:ui": "playwright test --project=chromium --ui"
  }
}
