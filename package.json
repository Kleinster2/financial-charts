{
  "name": "financial-charts",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  },
  "scripts": {
    "test:consistency": "python scripts/check_skill_parity.py --strict --optional-openclaw && python tests/note_compliance_regression.py",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:smoke": "playwright test --project=chromium",
    "test:smoke:ui": "playwright test --project=chromium --ui"
  }
}
