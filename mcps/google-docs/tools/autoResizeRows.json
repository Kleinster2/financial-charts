{
  "name": "autoResizeRows",
  "description": "Auto-resizes rows in a spreadsheet to fit their content. Optionally restrict to a row range (e.g., startRow=2, endRow=50); defaults to all rows.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "spreadsheetId": {
        "type": "string",
        "description": "The spreadsheet ID — the long string between /d/ and /edit in a Google Sheets URL."
      },
      "sheetName": {
        "type": "string",
        "description": "Name of the sheet/tab. Defaults to the first sheet if not provided."
      },
      "startRow": {
        "type": "integer",
        "minimum": 1,
        "description": "1-based start row index (inclusive). Omit to start from row 1."
      },
      "endRow": {
        "type": "integer",
        "minimum": 1,
        "description": "1-based end row index (inclusive). Omit to resize to the last row."
      }
    },
    "required": [
      "spreadsheetId"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}