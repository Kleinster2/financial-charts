{
  "name": "deleteEvent",
  "description": "Deletes an event from a Google Calendar. This is permanent — the event is removed, not trashed. Use sendUpdates to email cancellations to attendees.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "calendarId": {
        "type": "string",
        "default": "primary",
        "description": "Calendar ID. Defaults to \"primary\"."
      },
      "eventId": {
        "type": "string",
        "description": "The event ID to delete (from listEvents)."
      },
      "sendUpdates": {
        "type": "string",
        "enum": [
          "all",
          "externalOnly",
          "none"
        ],
        "default": "none",
        "description": "Whether to email cancellation notices to attendees."
      }
    },
    "required": [
      "eventId"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}