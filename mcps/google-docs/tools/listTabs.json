{
  "name": "listTabs",
  "description": "Lists all tabs in a document with their IDs and hierarchy. Use the returned tab IDs with other tools' tabId parameter to target a specific tab.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "documentId": {
        "type": "string",
        "description": "The document ID — the long string between /d/ and /edit in a Google Docs URL."
      },
      "includeContent": {
        "type": "boolean",
        "default": false,
        "description": "Whether to include a content summary for each tab (character count)."
      }
    },
    "required": [
      "documentId"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}